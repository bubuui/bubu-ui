import { popperProps } from '@bubu-ui/components/popper/src/popper-type';
import { ExtractPropTypes } from 'vue';

export const useTooltipProps = {};

export type ElTooltipProps = ExtractPropTypes<typeof useTooltipProps>;
